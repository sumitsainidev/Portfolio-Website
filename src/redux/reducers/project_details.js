import items from "../../components/Projects/projectItems/projectItems";

const initialState = {
  portfolios: items,
};

const project_details = (state, action) => {
  state = initialState;
  return state;
};

export default project_details;
